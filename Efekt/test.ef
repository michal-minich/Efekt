
let start = fn {

    var ax = 1
    //typeof(not)
    printLine("")

    let arr = [1, 2, 3, 4, 5]
    arrayIterator(arr)
      .where(fn c {c > 3 })
      .forEach(fn c { c.printLine() })

    var a = 0
    //a = 'x'
    loop {
         var b = 1
         a = a + 1
         printLine(a)
         if a == 1 then
             break
    }
}
