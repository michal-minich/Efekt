
import prelude

var start = fn {

    import prelude

    printLine(x)
    o.a(3).printLine()

    fib(0, 1, 1, 7)
}

var fib = fn a, b, counter, number {

    prelude.printLine(a)
    if counter < number then
        fib(b, a + b, counter + 1, number)
}