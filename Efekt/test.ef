

let start = fn {

    var voidFn = fn { }
    var voidFn2 = fn { id(1) }
    var void

    //id(void)
    //id(voidFn())

    //var y = fn {} ()
    //var y = voidFn()

    voidFn2()
}
